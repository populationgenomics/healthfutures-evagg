di_factory: lib.evagg.llm.LiteLLMClient
config:
  di_factory: lib.evagg.utils.get_dotenv_settings
  filter_prefix: "LITELLM_"
  # Optional configuration for connecting to custom endpoints like LiteLLM
  # base_url: "http://localhost:4000" # LiteLLM gateway URL
  # organization: "org-123456" # Optional organization ID